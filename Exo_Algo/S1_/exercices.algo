1.3) Quelles seront les valeurs des variables après exécution ?
	score = 6
	bonus = 2

1.4) cafe = 13
	lait=13

1.5) mon_age = 219
	ton_age = 219

	Pour switcher les valeurs entre personne ils faut passer passer par une variable temporaire afin de ne mas 	écraser la valeur mon_age;

1.6)
	alice = 4
	bob = 12
	tmp = 0
	tmp <- alice
	alice <- bob
	bob <- tmp
 
1.7)
     pour concaténer deux chaine de caractère il doit avoir un &, non + puisque ce ne sont pas des valeurs.
	